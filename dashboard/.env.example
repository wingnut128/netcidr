# Google OAuth 2.0 Web Client ID. Used as both the OIDC client_id and the
# audience the backend validates ID tokens against. Must match the
# `oauth_web_client_id` Terraform variable on the deploy stack.
VITE_OAUTH_WEB_CLIENT_ID=
